import React from "react";
import BentoMemberProfile from "../BentoMemberProfile";

const DetailMember = ({ user }: any) => {
  return <BentoMemberProfile user={user} />;
};

export default DetailMember;
